apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    environment: dev
    ports:
    - protocol:
      port: 80
      targetPort: 80