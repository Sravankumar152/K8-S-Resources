apiVersion: v1
kind: Pod
metadata:
  name: resource
  labels:
  - name: resource
  - environment: dev
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requets:
        memory: "64mi"
        cpu: "100m"
      limits:
        memory: "128mi"
        cpu: "150m"
